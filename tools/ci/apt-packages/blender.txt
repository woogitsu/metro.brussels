# Biblioteki systemowe dla jobów, które renderują w Blenderze headless.
# Ten plik jest KLUCZEM CACHE'A: zmiana listy unieważnia cache automatycznie,
# więc nie da się zapomnieć o jego przebiciu.
#
# SAMEGO BLENDERA TU NIE MA i to jest celowe. `apt-get install blender` na Ubuntu
# 24.04 daje 4.0.2 i będzie dawać 4.0.2 do końca życia wydania — a 4.0.2 renderuje
# LEGACY EEVEE, podczas gdy baseline projektu jest z EEVEE Next. Blender przychodzi
# z przypiętego tarballa przez `tools/ci/blender_install.sh`, razem z kontrolą
# sumy SHA-256 i z własnym Pythonem 3.13 oraz numpy 2.3.
#
# `python3-numpy` też tu był i też wypadł: był potrzebny WYŁĄCZNIE dlatego, że
# Blender z apt linkuje się z systemowym Pythonem. Żaden moduł w `tools/` ani `src/`
# nie importuje numpy poza Blenderem — sprawdzone grepem po całym drzewie.
#
# LISTA JEST PRZEPISANA, A NIE DOPISANA OBOK — 07.09.2026. Poprzednia wersja miała
# dwa pakiety (`libegl1`, `libgl1-mesa-dri`) i zdanie „zostają wyłącznie biblioteki,
# których tarball NIE wozi: kontekst EGL i programowy sterownik GL. Bez nich Blender
# startuje i wywraca się dopiero przy pierwszym renderze". Druga połowa tego zdania
# była NIEPRAWDZIWA: bez dziewięciu pakietów niżej Blender **nie startuje wcale**,
# a `libgl1-mesa-dri` nie dostarcza ani jednego pliku `libGL.so` (`dpkg -L` daje 0
# dopasowań) — wozi wyłącznie sterowniki DRI.
#
# Skąd te dziewięć. Zmierzone 07.09.2026 na przypiętym tarballu 5.2.1, suma SHA-256
# zgodna z `tools/ci/blender-version.txt`: `readelf -d` po wszystkich 201 plikach ELF
# w archiwum, odjęte 283 sonames dostarczane przez samo archiwum, a z pozostałych 28
# policzone DOMKNIĘCIE STARTOWE binarium `blender` — to, co ładowacz rozwiązuje przed
# `main()`. Rachunek i lista „czego NIE instalować" (backendy GPU denoisera i moduły
# Pythona, które startu nie blokują) stoją w `reports/biblioteki-startowe-blendera.md`.
#
# Powód, dla którego to wyszło dopiero teraz: stare maszyny puli miały te biblioteki
# z innych powodów, więc brak nigdy się nie ujawnił. Nowa pula `woogitsu-linux-*` ich
# nie ma i 07.09.2026 zatrzymało to dziewięć jobów naraz.
#
# `curl` dopisany 10.09.2026 (6.D77). Wołają go OBA instalatory — `blender_install.sh`
# po tarball i `godot_install.sh` po archiwum silnika — a nie był ani sondowany, ani
# w żadnym zestawie apt. Do 10.09.2026 działał wyłącznie dlatego, że maszyny puli
# miały go z innych powodów: dokładnie ta sama sytuacja, w której `unzip` przeszedł
# niezauważony do przebiegu 34155630333 i kodu 127. Pakiet i polecenie nazywają się
# tak samo, więc wpis w `POLECENIA_Z_PAKIETOW` jest tożsamościowy — i to nie czyni go
# zbędnym: bez niego bramka kolejności nie ma czego pilnować dla `curl`.
curl
libegl1
libgl1
libgl1-mesa-dri
libx11-6
libxext6
libxrender1
libxfixes3
libxi6
libxkbcommon0
libice6
libsm6
