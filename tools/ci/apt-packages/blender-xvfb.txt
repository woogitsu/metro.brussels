# Jak `blender.txt`, plus wirtualny X dla zrzutów z Godota.
# Samego Blendera tu nie ma — przychodzi z tarballa, powód w `blender.txt`.
#
# Lista bibliotek jest ta sama co w `blender.txt` i z tego samego pomiaru;
# różnicą są WYŁĄCZNIE `xvfb` i `unzip`, i tak to sprawdza
# `test_tool_installation_is_conditional_on_the_tool_being_missing`.
#
# `unzip` dopisany 07.09.2026, po tym jak `godot-first-run.yml` padł na
# `unzip: command not found` (kod 127, `woogitsu-linux-02`, run 34155630333).
# Krok pobiera Godota jako `.zip` z GitHuba; `curl` zadziałał, więc brakowało
# wyłącznie rozpakowywacza. Nie był ani sondowany, ani w żadnym zestawie apt.
#
# `curl` dopisany 10.09.2026 (6.D77), do OBU zestawów naraz, więc różnica między nimi
# zostaje ta sama. Wołają go oba instalatory — `blender_install.sh` po tarball
# i `godot_install.sh` po archiwum silnika — a nie był ani sondowany, ani w żadnym
# zestawie apt. Działał wyłącznie dlatego, że maszyny puli miały go z innych powodów:
# dokładnie ta sama sytuacja, w której `unzip` doszedł do kodu 127 dwa dni wcześniej.
curl
xvfb
unzip
libegl1
libgl1
libgl1-mesa-dri
libx11-6
libxext6
libxrender1
libxfixes3
libxi6
libxkbcommon0
libice6
libsm6
# `python3-yaml` dopisany 16.09.2026. Joby renderujące kończą się `[VERIFY]` na
# `tools/tests/test_all.py`, a pięć jego modułów importuje `yaml`
# (`test_ci_workflows`, `test_docs_ci_claims`, `test_engine_version`,
# `test_report_claims`, `test_timing_record`). PyYAML nie był ani sondowany, ani
# w żadnym zestawie — działał wyłącznie dlatego, że maszyny puli miały go z innych
# powodów. 15.09.2026 przestał i zatrzymał CZTERY joby naraz na trzech maszynach.
# Ta sama klasa co `unzip` (07.09.2026) i `curl` (6.D77): zależność realna,
# zadeklarowana nigdzie.
python3-yaml


# `libxcursor1` i `libwayland-cursor0` dopisane 16.09.2026 (6.D243). Godot z serwerem
# wyświetlania ładuje kursor DYNAMICZNIE, już po starcie — więc brak tych bibliotek
# nie zatrzymuje ani budowania, ani żadnego z czterdziestu kroków headless. Wychodzi
# dopiero na kroku 41, po siedmiu minutach pracy:
#     libXcursor.so.1: cannot open shared object file: No such file or directory
#     ERROR: Can't load XCursor dynamically.
#     libwayland-cursor.so.0: cannot open shared object file
#     ERROR: Unable to create DisplayServer, all display drivers failed.
# Zmierzone NA `main` (`bac8789`, run 35065478579, `docker-runner-04`), nie na gałęzi:
# na maszynach `metro-wsl-DOM-NEW-*` ten sam krok jest zielony, bo one te biblioteki
# mają z innych powodów. Piąty raz ta sama klasa, po `unzip` (07.09.2026), `curl`
# (6.D77), `python3-yaml` (6.D240) i `xz` (6.D242): zależność realna, zadeklarowana
# nigdzie. W OBU zestawach, bo lista sonames jest w całym drzewie jedna (6.D44), a jej
# identyczność jest ochroną przed sondą zawężoną — różnicą między zestawami
# zostają wyłącznie `xvfb` i `unzip`, jak dotąd.
libxcursor1
libwayland-cursor0
# `xz-utils` dopisany 16.09.2026 (6.D242) i jest to CZWARTY raz ta sama klasa,
# po `unzip` (kod 127, 07.09.2026), `curl` (6.D77) i `python3-yaml` (6.D240).
# `tools/ci/blender_install.sh` rozpakowuje tarball przez `tar -xJf`, a GNU tar
# NIE dekompresuje xz sam — uruchamia osobne binarium `xz`. Bez niego tar kończy
# `tar (child): xz: Cannot exec: No such file or directory`, i to jest komunikat,
# który zatrzymał TRZY joby naraz 16.09.2026 na `docker-runner-01/03/04`.
# ZALEŻNOŚĆ JEST NIEWIDOCZNA Z NAZWY: w skrypcie nie stoi napis `xz`, tylko flaga
# `-J`. Dlatego nie wystarczyło jej wypatrzeć okiem — pilnuje jej dziś bramka
# czytająca flagi kompresji z `tools/ci/*.sh`.
xz-utils
